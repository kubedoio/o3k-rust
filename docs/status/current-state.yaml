schema_version: 1
source_commit: 74cf5f6e83c92a1430815d23c796e099a0f1a198
release: v0.2.0-alpha.1
updated_at: 2026-08-02T17:30:33Z
last_protected_run:
  id: 30758733367
  commit: 74cf5f6e83c92a1430815d23c796e099a0f1a198
  result: failed

current_blocker:
  layer: guest
  summary: >-
    Protected bootstrap and authenticated command evidence now pass, but the
    first real public server create returned a compute-state conflict. The
    lifecycle therefore did not reach guest observation or console polling,
    and the process-boundary probe could not obtain a durable server record.
    The failed run also left owned OpenStack server and flavor records until
    workflow cleanup; foreign host state was unchanged.
  evidence: >-
    Protected workflow 30758733367 disposable-testlab-bootstrap.json,
    openstack-cli-result.json, compute-agent-mtls-result.json,
    compute-agent-process-mtls-result.json, resource-leak-result.json, and
    real-host-workflow-result.json.

completed:
  api_baseline: true
  nova_pre_dispatch: true
  agent_command_path: true
  server_active: false
  fixed_ip_projection: false
  cleanup_guard: false

pending:
  image_overlay_real_host: true
  config_drive_guest_consumption: true
  guest_network_connectivity: true
  console_boot_output: true
  restart_reconciliation: true

open_prs: []

next_actions:
  - Diagnose the protected compute-state conflict during the first public server create and preserve idempotent retry semantics.
  - Make the process-boundary inspect probe observe a durable server after the lifecycle creates it.
  - Re-run the protected lifecycle and prove image overlay, config-drive consumption, fixed IP, console, restart, and delete.
  - Resolve owned-resource cleanup after failed create without weakening foreign-state protection.
